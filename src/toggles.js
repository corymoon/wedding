export let showNav = true;
export let navFixed = true;

// toggle sections visibility
export let toggleSections = {
    // save the date
    saveTheDate: true,
    // our story
    story: true,
    // photos
    photos: true,
    // q & a
    qanda: false,
    // travel
    travel: false,
    // things to do
    activities: false,
    // registry
    registry: false,
    // rsvp
    rsvp: true,
};

// photos
export let showIndicators = false;

// rsvp
export let canRsvp = true;
